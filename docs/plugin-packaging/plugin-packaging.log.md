## `store-node-sqlite` — 2026-06-03 10:01:33 CEST

**Status:** done
**Summary:** Migrated the core task store off `better-sqlite3` to Node's built-in `node:sqlite` `DatabaseSync`, keeping `openTraceStore(databasePath)` and the `TaskStore` interface unchanged. The store now applies the existing Drizzle SQL migration files directly from the migration journal, maps SQLite rows into the existing public types, and keeps WAL/foreign-key setup behavior. Removed `better-sqlite3` and `@types/better-sqlite3` from `@trace/core` package metadata and lockfile importer/snapshots.
**Deviations:** The `/implement` resource templates were not present in the available skill/plugin directories, so this entry follows the existing Ralph log shape used elsewhere in the repo. `pnpm install --lockfile-only --offline` failed because the environment lacked cached metadata/tarballs for unrelated workspace packages and briefly removed the dev install; the lockfile was corrected manually and validated with a successful `pnpm install --filter @trace/core --prod --offline`. After the dev install was lost, Vitest/tsc could not be rerun because offline dev reinstall is blocked by a missing cached `js-yaml` tarball from the ESLint dependency chain.
**Handoff:** Red/green before the dev install was lost: `pnpm --filter @trace/core test -- task-store.test.ts` failed on the native dependency assertion, then the focused store suite passed after the initial node-sqlite adapter work; `task-docs.test.ts` and `token-totals.test.ts` also passed, and `@trace/core` typecheck passed at that point. After replacing the remaining Drizzle better-sqlite3 runtime import with direct `node:sqlite` SQL, verified with `node --experimental-strip-types --check` for `store.ts`, `task-store.test.ts`, `task-docs.test.ts`, and `token-totals.test.ts`; direct public-API node-sqlite smoke tests for fresh DB create/register/assign and old-schema read/write continuity; migration journal path validation; `git diff --check`; no direct native package/import references outside Drizzle's optional peer metadata and the package assertion test; and successful `pnpm install --filter @trace/core --prod --offline`.
