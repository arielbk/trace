## `store-node-sqlite` — 2026-06-03 10:01:33 CEST

**Status:** done
**Summary:** Migrated the core task store off `better-sqlite3` to Node's built-in `node:sqlite` `DatabaseSync`, keeping `openTraceStore(databasePath)` and the `TaskStore` interface unchanged. The store now applies the existing Drizzle SQL migration files directly from the migration journal, maps SQLite rows into the existing public types, and keeps WAL/foreign-key setup behavior. Removed `better-sqlite3` and `@types/better-sqlite3` from `@trace/core` package metadata and lockfile importer/snapshots.
**Deviations:** The `/implement` resource templates were not present in the available skill/plugin directories, so this entry follows the existing Ralph log shape used elsewhere in the repo. `pnpm install --lockfile-only --offline` failed because the environment lacked cached metadata/tarballs for unrelated workspace packages and briefly removed the dev install; the lockfile was corrected manually and validated with a successful `pnpm install --filter @trace/core --prod --offline`. After the dev install was lost, Vitest/tsc could not be rerun because offline dev reinstall is blocked by a missing cached `js-yaml` tarball from the ESLint dependency chain.
**Handoff:** Red/green before the dev install was lost: `pnpm --filter @trace/core test -- task-store.test.ts` failed on the native dependency assertion, then the focused store suite passed after the initial node-sqlite adapter work; `task-docs.test.ts` and `token-totals.test.ts` also passed, and `@trace/core` typecheck passed at that point. After replacing the remaining Drizzle better-sqlite3 runtime import with direct `node:sqlite` SQL, verified with `node --experimental-strip-types --check` for `store.ts`, `task-store.test.ts`, `task-docs.test.ts`, and `token-totals.test.ts`; direct public-API node-sqlite smoke tests for fresh DB create/register/assign and old-schema read/write continuity; migration journal path validation; `git diff --check`; no direct native package/import references outside Drizzle's optional peer metadata and the package assertion test; and successful `pnpm install --filter @trace/core --prod --offline`.

## `cli-bundle` — 2026-06-03 10:09:20

**Status:** done
**Summary:** Added a dependency-free `@trace/cli` build command that emits executable JS artifacts for `trace` and the Claude `SessionStart` hook under `apps/cli/dist`. The builder uses Node's built-in TypeScript stripping, embeds the CLI and `@trace/core` source modules into each artifact, rewrites workspace-relative imports to materialized temp modules at runtime, and no longer needs the repo source tree or native dependencies to run. Moved migration journal/SQL access from filesystem reads to bundled core constants so migrations travel inside the artifact.
**Deviations:** The `/implement` resource templates were not present in the available skill/plugin directories, so this entry follows the existing Ralph log shape. A full `pnpm install`/Vitest/typecheck/lint pass could not be restored after pnpm removed `node_modules`: offline install lacks cached metadata for `eslint-config-prettier`, and networked install cannot resolve `registry.npmjs.org`. To keep the slice verifiable in this environment, the bundle smoke test uses Node's built-in test runner and the build path has no new package dependencies.
**Handoff:** Verified `pnpm --filter @trace/cli build`; `pnpm --filter @trace/cli test:bundle`; direct hook artifact smoke registering a Claude session and reading it back via the bundled CLI; `node --check` for `apps/cli/src/build.ts`, `apps/cli/src/bundle.test.ts`, `packages/core/src/migrations.ts`, and `packages/core/src/store.ts`; migration constants exactly match `packages/core/drizzle/meta/_journal.json` and each SQL file; generated artifacts contain no `@trace/core`, `better-sqlite3`, `packages/core/drizzle`, or `migrationsDir` references; and `git diff --check`.

## `plugin-scaffold` — 2026-06-03 10:15:00 CEST

**Status:** done
**Summary:** Added the repo-level Claude Code plugin scaffold: `.claude-plugin/plugin.json`, `hooks/hooks.json` with a `SessionStart` command using `${CLAUDE_PLUGIN_ROOT}`, the existing trace skill updated to invoke the bundled plugin CLI instead of a PATH `trace`, and build output copies under `bin/` for the CLI and hook artifacts. The CLI build now writes executable artifacts both to `apps/cli/dist` and to the plugin `bin/` directory, and the root package declares ESM so plugin bundle execution is quiet.
**Deviations:** The `/implement` resource templates were still not present in the available skill/plugin directories, so this entry follows the established Ralph log shape. The slice feedback loop is documented as manual plugin installation; I added an automated structural test and direct hook smoke to cover the installable file layout in this AFK environment, without marking the slice `needs-review`.
**Handoff:** Verified red/green with `node --test apps/cli/src/plugin-scaffold.test.ts`; existing bundle smoke with `pnpm --filter @trace/cli test:bundle`; JSON parsing for `.claude-plugin/plugin.json` and `hooks/hooks.json`; `node --check` for `apps/cli/src/build.ts` and `apps/cli/src/plugin-scaffold.test.ts`; direct bundled CLI probe with sandbox-local `TRACE_DB`; direct bundled SessionStart hook probe registering `plugin-probe` and reading it back via `bin/trace.js session list --unassigned`; generated plugin artifacts are executable and contain no `@trace/core` or `better-sqlite3` references.

## `marketplace` — 2026-06-03 10:17:53 CEST

**Status:** needs-review
**Summary:** Added `.claude-plugin/marketplace.json` so the GitHub repo can be added as a Claude Code marketplace and exposes the existing `trace` plugin from the repository root. Added a focused marketplace contract test that verifies the marketplace name, owner, single plugin entry, relative source path, and alignment with the plugin manifest and stable plugin files.
**Deviations:** The `/implement` resource templates were still not present in the available skill/plugin directories, so this entry follows the established Ralph log shape. The slice's feedback loop is manual and the slice has `Human checkpoint: yes`; automated validation is complete, but a clean Claude Code marketplace-add/install and full hero loop still need final human QA, so the slice is marked `needs-review`.
**Handoff:** Verified red/green with `node --test apps/cli/src/marketplace.test.ts` failing on the missing marketplace manifest, then passing after adding it. Also verified `node --test apps/cli/src/plugin-scaffold.test.ts`, `node --check apps/cli/src/marketplace.test.ts`, `claude plugin validate .`, direct JSON/path checks for marketplace/plugin/hooks/skill/bundled artifacts, and `git diff --check`.

## `retire-init-and-docs` — 2026-06-03 10:22:42

**Status:** done
**Summary:** Retired `trace init` as a hook-writing installer while keeping it as a diagnostic compatibility command that points users to plugin marketplace install. The CLI dispatcher now handles `init` before resolving or opening the trace store, so it does not need `HOME`, `TRACE_DB`, or a settings path. README and the trace skill setup section now document plugin installation, and the rebuilt CLI/plugin bundles include the new behavior.
**Deviations:** `pnpm --filter @trace/cli test -- installer.test.ts` still cannot run because `vitest` is absent from this workspace's `node_modules`; the installer regression was converted to Node's built-in test runner and verified there.
**Handoff:** Verified `node --test apps/cli/src/installer.test.ts apps/cli/src/plugin-scaffold.test.ts apps/cli/src/marketplace.test.ts apps/cli/src/bundle.test.ts`; `pnpm --filter @trace/cli build`; `pnpm --filter @trace/cli test:bundle`; direct bundled `env -u HOME -u TRACE_DB node bin/trace.js init` diagnostic output with no settings/store requirement; `node --check` for `installer.ts`, `installer.test.ts`, `trace.ts`, and `task-crud.test.ts`; README and `skills/trace/SKILL.md` contain no `pnpm link --global` or `trace init` setup references; stale hook-writing messages are absent from `apps/cli/src`, `bin`, README, and the trace skill; and `git diff --check`.
