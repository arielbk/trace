# Core Deepening — Implementation Log

## `token-totals` — 2026-05-30 03:45:05

**Status:** done
**Summary:** Added `packages/core/src/token-totals.ts` — a deep TokenTotals value module exporting `emptyTokenTotals()`, `addTokenTotals(a, b)`, and `tokenTotalsFromUsage(raw)` (with the `total ?? sum-of-parts` derivation and snake_case/camelCase usage-key handling). Both transcript adapters (`claude-code-adapter`, `codex-adapter`) and `store.ts` now consume it instead of their three private copies of the same math. All three functions are re-exported from `index.ts`, plus a `RawTokenUsage` type.
**Deviations:** Function names are prefixed (`emptyTokenTotals`/`addTokenTotals`/`tokenTotalsFromUsage`) rather than bare `empty`/`add`/`fromUsage` from the slice sketch — these are top-level named exports from the package barrel, so unprefixed names would be ambiguous. The merged usage-normalizer is `tokenTotalsFromUsage` (it subsumes the store's former `normalizeTokenTotals`, which accepted camelCase `Partial<TokenTotals>` — `RawTokenUsage` accepts both casings so that input still works).
**Handoff:** `ClaudeCodeTokenTotals` and `CodexTokenTotals` are now plain aliases of `TokenTotals` (kept so existing re-exports/consumers don't break — they're structurally identical). The adapters accumulate via `addTokenTotals(acc, tokenTotalsFromUsage(usage))` per event, which is behavior-equivalent to the old in-place `addUsage` (undefined usage → empty → no-op). The `transcript-adapters` slice (depends on this one) should build its per-tool adapter interface on top of this module rather than reintroducing token math. Feedback loop verified: core 31/31 + types + lint clean; CLI 24/24 still green.

## `transcript-adapters` — 2026-05-30 03:53:26

**Status:** done
**Summary:** Introduced `packages/core/src/transcript-adapter.ts` — a `TranscriptAdapter` interface per `SessionTool` plus a `getTranscriptAdapter(tool)` registry, each adapter answering `parse`/`parseFile` (id, model, token totals) and `tail`/`readTail` (message tail) for one tool. Extracted the shared tail harness + role/text normalizers into `transcript-messages.ts`; moved the per-tool event→message extraction into `claude-code-adapter.ts` (`tailClaudeCodeTranscript`) and `codex-adapter.ts` (`tailCodexTranscript`). `transcript-tail.ts` is now a thin back-compat shim delegating to the registry, and the CLI `session tail` consults `getTranscriptAdapter(session.tool).readTail(...)` instead of importing the free `readTranscriptTail`.
**Deviations:** The unified `TranscriptAdapter.parse` takes `expectedId` (codex maps it to `expectedThreadId`; claude ignores it) since the two tools' parse signatures differed. `scanCodexSessions` stays a free codex-only export — it's a directory scan, not "id/model/tokens/tail from a transcript", so it's outside the per-tool interface; CLI `session scan --codex` is unchanged. The old `tailTranscriptMessages`/`readTranscriptTail` exports are kept (now shims) so existing `transcript-tail` tests and any external callers don't break.
**Handoff:** The tool-string branch that used to live inside `tailTranscriptMessages` is gone — dispatch is now a registry lookup keyed by `SessionTool`, so adding a third tool means implementing one `TranscriptAdapter` (parse + tail) and adding one entry to `adaptersByTool`, not scattering new free functions + new branches. `ParsedTranscript` (in transcript-adapter.ts) is the generic shape; `ParsedClaudeCodeSession`/`ParsedCodexSession` remain the concrete per-adapter return types and are structurally compatible. `transcript-messages.ts` owns the shared `collectTranscriptTail` harness (best-effort, returns `[]` on any parse failure) and `normalizeRole`/`textFromContent`/`isObject` — reuse it for any future per-tool extractor. Feedback loop verified: core 34/34, CLI 24/24, both packages typecheck + lint clean.

## `session-identity` — 2026-05-30 04:08:26

**Status:** done
**Summary:** Added `packages/core/src/session-identity.ts` exporting `inferSessionIdentity(env, overrides?)` — the single home for the cross-tool "which env var is the live session" contract (codex `CODEX_THREAD_ID`; claude `CLAUDE_CODE_SESSION_ID` with legacy `CLAUDE_SESSION_ID`/`session_id` fallbacks; per-tool transcript-path env vars + `tool:id` synthesis). The CLI's three inline helpers (`inferCurrentTool`/`inferCurrentSessionId`/`inferTranscriptPath`) are deleted; `parseSkillWorkOnTaskArgs` now layers its `--id`/`--transcript`/`--tool` flags on as overrides to one core call.
**Deviations:** Collapsed the three CLI helpers into one overridable `inferSessionIdentity` rather than lifting three separate functions — the slice's "takes an env map and returns tool + id + transcript path" wants a single seam, and the optional `overrides` ({tool, id, transcriptPath}) preserves the CLI's per-flag override semantics exactly. `SessionIdentity.id`/`.transcriptPath` are `string | undefined` (undefined together when no session env is present); the CLI's existing "requires --id" guard narrows both. `--tool` string validation ("Session tool must be claude or codex") stays in the CLI (it's flag parsing), now done before the core call so `overrides.tool` is typed `SessionTool`.
**Handoff:** `inferSessionIdentity` is the seam `cli-installer-split` (depends on this) should use for the SessionStart hook's session identity instead of any inline CLI logic — pass the hook's env (and any explicit overrides) and read `{tool, id, transcriptPath}`. Invariant now in place: transcript-path synthesis format `${tool}:${id}` and the legacy claude-id fallback order live only in this module — don't reintroduce them elsewhere. Feedback loop verified: core 45/45 (11 new session-identity tests), CLI 24/24, both packages typecheck + lint clean.

## `task-docs` — 2026-05-30 04:21:34

**Status:** done
**Summary:** Added `packages/core/src/task-docs.ts` — the named home for the dual-source doc contract: `resolveTaskDocsDir`, `listNativeTaskDocs(databasePath, taskId)` (reads the trace-native docs dir, ENOENT → `[]`), and `mergeTaskDocs(registered, native)` (union, dedup-by-path with the registered/DB doc winning, then sort by createdAt → path). `store.ts#listDocsForTask` now queries the DB for registered docs and delegates the merge to `mergeTaskDocs(registeredDocs, listNativeTaskDocs(...))`; its inline map/dedup/sort and its private `listNativeTaskDocs` copy are gone.
**Deviations:** none from the slice intent. Moved `resolveTaskDocsDir` out of `store.ts` into `task-docs.ts` (store now re-exports it via `export { resolveTaskDocsDir } from "./task-docs.ts"`) so all docs-path/native-read/merge logic lives in one module; `index.ts` still re-exports it through `store.ts`, so the public barrel is unchanged. The slice sketch named a "TaskDocs module ... with the SQLite store as one source behind a seam" — the seam is the function boundary: the store supplies the DB-queried `registered` array as the first arg, the module owns merge + native-dir reading. No `listTaskDocs`-style facade was added since `mergeTaskDocs` is the testable unit the feedback loop calls for and the store already holds the DB handle.
**Handoff:** The merge/dedup/ordering rule (registered wins on path collision; order = createdAt asc then path asc) now lives only in `mergeTaskDocs` — don't reintroduce it in callers. Any future doc consumer should call `mergeTaskDocs` with its own registered source rather than re-implementing the union. `resolveTaskDocsDir` and `listNativeTaskDocs` are exported from `task-docs.ts` (the former re-exported through `store.ts`/`index.ts`); `listNativeTaskDocs` is not on the public barrel — add it there if `cli-installer-split` or another slice needs native-only listing. Feedback loop verified: core 48/48 (3 new task-docs tests), CLI 24/24, both packages typecheck + core lint clean.

## `cli-installer-split` — 2026-05-30 04:29:08

**Status:** done
**Summary:** Extracted the `trace init` installer out of `apps/cli/src/trace.ts` into a new `apps/cli/src/installer.ts` (exports `runInit(env, cwd)`; owns settings-path resolution, idempotent SessionStart-hook wiring, the `ClaudeSettings` shape, hook-path + trace-skill-path resolution). `trace.ts`'s `init` branch now just calls `runInit` and lost its `node:fs`/`node:path` install-time imports. The Claude SessionStart hook (`claude-session-start-hook.ts`) now resolves the registered session through core `inferSessionIdentity` (treating its stdin `session_id`/`transcript_path` as overrides with `tool: "claude"`) instead of hardcoding the tool string inline.
**Deviations:** none from the slice intent. The hook keeps its existing stdin-contract guards (still rejects missing `session_id`/`transcript_path` with the same messages) and only routes the *resolved* identity into `session register` — so observable behavior is unchanged; the seam consumption is structural. Register args use `identity.id ?? input.session_id` / `identity.transcriptPath ?? input.transcript_path` purely to satisfy the `string | undefined` return type after the truthy guards have already narrowed the inputs (the `??` never actually falls back, since the overrides are passed verbatim).
**Handoff:** All `trace init` / settings.json wiring now lives only in `installer.ts` — don't reintroduce settings-path/hook-path logic in `trace.ts`. `resolveClaudeSettingsPath`/`readClaudeSettings`/`resolveClaudeHookPath`/`resolveTraceSkillPath` are module-private; export them from `installer.ts` if a future slice needs them directly. The SessionStart hook is now a thin consumer of the core session-identity seam (the last inline tool-string lived here); any new hook/entry point should likewise call `inferSessionIdentity` rather than assuming a tool. New unit coverage: `installer.test.ts` (4 tests, drives `runInit` directly against a temp HOME — no CLI spawn) and `claude-hook-identity.test.ts` (2 tests, in-process hook → register round-trip + missing-`session_id` guard). Feedback loop verified: CLI 30/30 (was 24/24), typecheck + lint clean. This was the last slice — every slice in the tasks file is now `done`.
