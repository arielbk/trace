# Task legibility — implement log

## `board-declutter` (review cycle) — 2026-06-06 14:52:00

**Status:** done (approved by user)
**Summary:** Resolved the `board-declutter` human checkpoint and three issues that surfaced during interactive review:
1. **Stale served bundle** — `trace serve` serves `bin/web` (copied from `apps/web/dist` by the CLI build), not `apps/web/dist` directly. `pnpm build` was leaving `bin/web` stale because `apps/cli` didn't declare `@trace/web` as a dependency, so Turbo neither ordered the web build first nor invalidated the CLI build cache on web changes. Fixed by adding `"@trace/web": "workspace:*"` to `apps/cli` devDependencies (build-ordering dep; the CLI copies web's dist, never imports it). Verified end-to-end: a web change now propagates into `bin/web` via a plain `pnpm build`.
2. **Detail-page header polish** — moved the "Copy re-enter prompt" chip onto the title's row (new `.task-title-row`, flex `space-between`); description now sits directly under the title; `.task-heading` is now a column; bumped `.token-summary` top margin `--space-4` → `--space-5`. Removed the now-unneeded `flex-basis: 100%` from `.task-description`.
**Deviations:** During the build-fix verification I ran `git checkout apps/web/src/styles.css` to drop a probe line — this reverted the WHOLE file, silently discarding the uncommitted board CSS (`.task-row-description`, the `.task-row-slug` removal). Recovered fully by extracting the pre-revert built CSS from Turbo's cache (`.turbo/cache/*.tar.zst`), diffing selectors against the current build, and restoring the exact three lost edits. Confirmed via full-CSS selector diff that nothing else was lost. Lesson: never `git checkout <file>` while uncommitted slice work is in the tree.
**Handoff:** All work is in the working tree, UNCOMMITTED (nothing has been committed this session). `board-copy-action` is the only remaining slice. TWO reviewer nits are still OPEN and should be folded into `board-copy-action` (or a cleanup commit): (a) `apps/web/src/format.ts:103-104` — `collapseHomePath` JSDoc still claims a `VITE_HOME`/`HOME` default, but the real default is `""` and the runtime source is now `/api/config`; the JSDoc is misleading and should be corrected. (b) `packages/core/src/api-handler.ts` — `/api/config` accepts any HTTP method and returns 200; the other routes gate-check methods, so add a `methodNotAllowed()` for non-GET for consistency. Verified this session: full web suite 95 tests pass, check-types clean.

## `humanize-titles` — 2026-06-06 13:47:58

**Status:** done
**Summary:** `createTask` now humanizes slug-shaped titles: `looksLikeSlug` (kebab-case with ≥1 dash, UUIDs excluded) plus `humanizeSlug` (dashes→spaces, capitalize first letter) in `packages/core/src/slug.ts`; the original string round-trips through `slugify` so it remains the slug verbatim. Ordinary titles and single lowercase words pass through unchanged.
**Deviations:** none
**Handoff:** Single lowercase words ("checkout") are deliberately NOT slug-shaped — only strings with at least one dash qualify, and mixed case disqualifies. UUID titles keep the existing placeholder-slug path (`task-<shortid>`) and are not humanized; `looksLikeSlug` excludes them internally, so callers don't need to re-check. The humanize→slugify round-trip is exact for any string matching SLUG_PATTERN ≤60 chars. Verified: core 106 tests, CLI 71 tests, workspace check-types + lint all pass.

## `detail-copy-prompt` — 2026-06-06 13:57:14

**Status:** done
**Summary:** Added `buildReEnterPrompt(title, slug)` pure function to `apps/web/src/format.ts` (output: `Re-enter the trace task "Title" (slug)`); 2 unit tests in `format.test.ts`. Detail header now shows title, optional description, and a `CopyChip` with `display="Copy re-enter prompt"` — slug line and UUID chip removed from `TaskPage.tsx`. The `truncateId` import was also removed since it is no longer used in that file.
**Deviations:** `renderToStaticMarkup` HTML-encodes double-quotes in `title` attributes (`&quot;`), so the test checks the encoded form rather than the raw string.
**Handoff:** `buildReEnterPrompt` is exported from `apps/web/src/format.ts` and should be imported by `board-copy-action` when it adds the hover row-action. The CopyChip `display` prop is deliberately human-readable ("Copy re-enter prompt"), not the prompt text itself — the prompt text rides in `value` (and therefore in the chip's `title` attribute and clipboard). Session/doc chips already displayed filename-only via `truncatePath` before this slice — no change needed there. Full workspace 581 tests pass, check-types clean.

## `board-declutter` — 2026-06-06 14:31:00

**Status:** needs-review
**Summary:** Added `/api/config` route to `handleTraceApiRequest` in `packages/core/src/api-handler.ts`; it returns `{ home: os.homedir() }` as JSON (200) with no database access. Unit test added to `packages/core/src/api-handler.test.ts` (verifies status 200, JSON content type, and that `home` is a non-empty string). `TasksPage` now fetches `/api/config` alongside `/api/tasks` in a single `useEffect` and passes `home` state into `<TaskList home={home}>`. Component-level work (`TaskList` `home` prop, `~`-collapse display, description row, no UUID chip, no slug text) was already present in the working tree and tested. Also fixed pre-existing lint violation in `apps/web/src/format.test.ts` where three `collapseHomePath` tests used `require()` instead of the already-imported ES identifier — replaced with direct calls to the module-level import.
**Deviations:** Fixed a pre-existing lint failure (`@typescript-eslint/no-require-imports` in `format.test.ts`) that was blocking `npm run lint`; this was introduced by the `board-declutter` working-tree changes before this slice ran, not by a prior slice.
**Handoff:** `buildReEnterPrompt` (from `apps/web/src/format.ts`) is available for `board-copy-action`. The `/api/config` endpoint is lightweight (no DB, no method restriction); the `board-copy-action` slice does not need it. Manual verification needed: board shows `~`-collapsed paths in group headers, chips still copy the full absolute path, rows show descriptions clamped to one line, no kebab slugs or UUID chips visible. Verified: 588 tests pass, check-types clean, lint clean.

## `slug-resolution` — 2026-06-06 13:53:37

**Status:** done
**Summary:** Skill verbs (`re-enter`, `work-on-task`) now resolve refs via a shared `resolveSkillTaskRef` in `apps/cli/src/trace.ts`: task id first, then slug exact, then normalized-exact title (trimmed, case-insensitive). A miss exits 1 with `Task not found: <ref>` plus up to 5 near candidates (`slug — title`, plain containment match on slug/lowercased title). `findTaskIdByTitle` (case-sensitive exact title) is gone. SKILL.md updated: slug is the canonical re-enter ref in examples, plus a sentence-case-titles nudge under work-on-task; frontmatter description mentions slug.
**Deviations:** Found `apps/cli` npm script `test:bundle` (`node --test src/bundle.test.ts`) is broken pre-existing — bundle.test.ts imports vitest and dies under node's runner; the vitest suite runs it fine (passes). Not fixed; out of slice scope.
**Handoff:** `work-on-task` behavior change: it now also resolves by slug and case-insensitive title before creating, so a slug-shaped or case-variant ref binds the existing task instead of minting a duplicate (composes with `humanize-titles`). Resolution precedence is id > slug > normalized title; candidates list is containment-only, no fuzzy semantics (vague refs stay with the recall skill). Bundles `bin/trace.js` + `apps/cli/dist/*` regenerated via `node src/build.ts` — keep doing this after any trace.ts change, the bundle test compares them. Verified: full workspace vitest 579 passing (incl. 4 new CLI tests + 1 prose test), check-types + lint pass.

## `board-copy-action` — 2026-06-06 14:59:16

**Status:** done
**Summary:** Board rows now reveal a quiet copy-re-enter-prompt icon action on hover, alongside archive/unarchive, following the existing hover-swap pattern. New `CopyPromptAction` icon button (`apps/web/src/pages/TasksPage.tsx`) copies `buildReEnterPrompt(task.title, task.slug)` — the same builder the detail page uses — and swaps its icon to a check for a beat after copy. Both actions are wrapped in a new `.task-row-actions` flex container (absolute, swaps in over the relative-time slot); the CSS that previously targeted a single `.task-row-action` now targets the container. Extracted the clipboard-with-confirm logic from `CopyChip` into a shared `useClipboardCopy` hook (`apps/web/src/components/useClipboardCopy.ts`) so the chip and the new action confirm copies identically. Also folded in the two open reviewer nits from `board-declutter`: corrected the stale `collapseHomePath` JSDoc (default is `""`, runtime source is `/api/config`, not `VITE_HOME`/`HOME`) and added a non-GET 405 guard to `/api/config` in `packages/core/src/api-handler.ts` (with a core test).
**Deviations:** Slice scope expanded to clear the two `board-declutter` reviewer nits (JSDoc fix + `/api/config` method gating) per that slice's handoff. `renderToStaticMarkup` HTML-encodes the quotes in the prompt `title` attribute (`&quot;`), so the row test asserts the encoded form, matching `detail-copy-prompt`.
**Handoff:** All slices are now `done`. Work is UNCOMMITTED in the working tree (nothing committed this session — same as prior slices). `bin/web` was rebuilt via `pnpm build` so `trace serve` reflects the new board; the served JS/CSS hashes are `index-hA-mTFX7.js` / `index-wB_X22cx.css`. No CLI/`trace.ts` change this slice, so no `bin/trace.js` bundle regen was needed. The copy action renders on every row (active and archived) since re-entering an archived task is valid; for untitled rows the prompt carries an empty title but the slug still resolves. Manual verification (no automated coverage for click+clipboard — no jsdom in this app): hover a board row, click the copy icon, paste into a fresh Claude session, confirm it binds to the right task; archive action still works beside it. Verified: full workspace 265 tests pass (108 core + 81 web + 76 cli), lint + check-types clean across all three packages.
