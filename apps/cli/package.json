{
  "name": "@trace/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "trace": "./src/trace.ts"
  },
  "scripts": {
    "build": "node src/build.ts",
    "test:bundle": "node --test src/bundle.test.ts",
    "test": "vitest run",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@trace/core": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@trace/web": "workspace:*",
    "@types/node": "^22.15.3",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^4.1.7"
  }
}
