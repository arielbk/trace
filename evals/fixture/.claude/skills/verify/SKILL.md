---
name: verify
description: Verify that a code change actually does what it's supposed to by running the app and observing behavior. Always invoke when asked to "confirm this fix works", "confirm a fix works by running the app", verify a PR, or validate local changes before pushing.
---

Decoy stub — routing test only.
